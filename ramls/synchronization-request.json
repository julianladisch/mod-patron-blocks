{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "Synchronization request schema",
  "properties": {
    "scope": {
      "type": "string",
      "description": "Scope of re-sync data. Format: [user:{user-uuid}] or [full] for all users"
    }
  },
  "additionalProperties": false,
  "required": [
    "scope"
  ]
}
